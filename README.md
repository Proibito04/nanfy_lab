
  # Generate CSS Variables

  This is a code bundle for Generate CSS Variables. The original project is available at https://www.figma.com/design/zpyxiqIQqoByFkCaLh4Er1/Generate-CSS-Variables.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  